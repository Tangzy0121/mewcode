// Package tools defines the tool interface and a registry that exposes tool
// definitions to the model and dispatches calls by name. It implements the six
// built-in tools: read, write, edit, bash, glob and grep. Implemented in later
// tasks.
package tools
