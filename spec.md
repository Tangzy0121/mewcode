# MewCode — Spec(本轮:最小可跑内核)

> 角色:回答"要解决什么问题、做哪些能力、不做哪些、什么算完成"。
> 本文件刻意**不写**具体函数名/参数名/默认值/错误文本/行号/SDK 类型名——那些是实现细节,放在 plan.md 与 checklist.md。

## 背景

MewCode 是一个用 **Go** 编写的终端 AI 编程助手,定位与 Claude Code 同类:在终端里和大模型多轮对话,模型可以调用工具读写代码、执行命令,从而把"问答"升级为"能动手的智能体"。

本项目按里程碑分轮迭代,**本轮只交付"最小可跑内核"**:一个能在终端跑起来、能对话、能调全套工具、有权限把关、有基础 TUI 的可用程序。会话持久化、上下文压缩、多 provider 落地、富渲染等留给后续轮次。

> 注:本设计参照一份网上教程构建,后续会与教程实现做对照复盘(批评与自我批评),因此文档需让"我们的取舍点"清晰可见。

## 目标用户

- 项目作者本人:借造轮子理解 agentic coding 助手的内部机制(对话循环、tool-use 协议、权限模型、TUI 并发)。
- 想要一个可定制、可接入国内模型(DeepSeek 等)的轻量终端助手的开发者。

## 能力清单(一句话一条)

- 在终端启动后进入交互式会话,支持多轮对话。
- 模型回复以流式逐字呈现,不必等整段生成完。
- 模型可调用工具读取指定文件的内容。
- 模型可调用工具创建或整体覆盖写入文件。
- 模型可调用工具对文件做精确的局部替换式修改。
- 模型可调用工具执行 shell 命令并取回输出。
- 模型可调用工具按文件名模式查找文件。
- 模型可调用工具按内容在代码库中搜索。
- 有副作用的工具(写文件、执行命令)在执行前经过权限把关。
- 权限分三档模式,启动时可选,运行中可切换。
- 工具调用在界面上以独立卡片呈现其运行状态(运行中/成功/失败)。
- 需要批准的操作在界面上弹出可选择的确认项。
- 模型后端通过统一接口抽象,本轮落地一个后端,接口为第二个后端预留位置。
- API 报错、工具出错、权限被拒均不致使程序崩溃,而是反馈给模型或用户后继续。

## 非功能要求

- **稳定性**:任何单次 API 失败、工具失败、权限拒绝都不能让程序退出或卡死。
- **并发安全**:智能体主循环与界面渲染循环之间只通过消息传递通信,不共享可变状态。
- **可测性**:模型后端可用一个假实现替换,使核心循环与工具不依赖真实网络即可测试。
- **可移植性**:本轮主力平台为 Windows;命令执行需适配 Windows 默认 shell。
- **可扩展性**:新增工具、新增模型后端都应是"实现一个接口 + 注册",不改动主循环骨架。
- **可观测性**:工具的输入与执行结果、权限决策对用户可见。

## 设计骨架(七个单元,各自单一职责)

| 单元 | 职责 | 对外暴露 | 依赖 |
| ---- | ---- | -------- | ---- |
| 入口 | 解析启动参数、选权限模式、装配组件、启动界面 | 可执行程序 | 全部 |
| config | 读取模型凭证、模型名、后端地址等配置 | 一个配置对象 | 无 |
| llm | 抽象"模型后端"接口并落地一个实现:流式收发、tool-use 协议、分层错误 | 后端接口 + 流式事件 | config, conversation |
| conversation | 两层消息模型:Manager 持有内存对话历史(文本/工具调用/工具结果),按后端 protocol 序列化 | 消息类型 + 管理器 | 无 |
| agent loop | 智能体主循环:追加历史→请求后端→消费流→遇工具调用则派发→收集结果回灌→直到本轮结束;历史委托给 conversation.Manager | 启动/输入/事件的接口 | llm, conversation, tools, permission |
| tools | 工具注册表与各工具实现:每个工具自带名称、入参模式、执行逻辑 | 工具注册表 + 工具接口 | permission |
| permission | 三档权限模式与逐次放行判定;需批准时产出一个待决请求 | 判定接口 | 无 |
| tui | 终端界面:输入框、可滚动对话区、流式渲染、工具卡片、权限弹选;经消息与主循环通信 | 界面程序 | agent loop |

### 数据流(高层)

用户输入 → 界面把提示交给主循环 → 主循环追加历史并请求后端 → 后端产出事件流(文本增量 / 工具调用块) → 文本增量实时渲染;工具调用先过权限判定(需批准则发请求给界面等用户选择,放行则直接执行)→ 工具结果(成功/出错/被拒)回灌历史 → 再次请求后端,循环至本轮结束。

### 关键设计取舍(供后续与教程对照)

- **后端先抽象再落地**:即便本轮只实现一个后端,对话循环也按接口编写,避免第二个后端到来时返工。
- **主循环与界面解耦**:用消息传递而非共享状态,换取并发安全与可测性,代价是多一层接缝。
- **权限内建于工具派发路径**:把关是循环的固定环节,而非工具自觉,保证无法绕过。
- **工具结果即模型输入**:工具出错与权限被拒都转成模型能读懂的反馈,让模型自我纠错而非中断。

## 什么算完成(本轮)

- 在终端启动 MewCode,输入一句涉及"读代码并回答"的请求,模型能调读/搜工具、流式给出答案。
- 输入一句涉及"改代码"的请求,在 default 权限下弹出确认,批准后文件被正确修改。
- 输入一句涉及"跑命令"的请求,权限放行后命令执行且输出回灌、模型据此继续。
- 全程任一失败不崩溃;退出后会话不保留(本轮预期行为)。
- 详尽、可勾选的验收项见 checklist.md。

## Out of Scope(本轮明确不做)

- 会话持久化与 `--resume` 恢复。
- 上下文接近窗口上限时的自动摘要/压缩。
- 第二个模型后端(OpenAI 兼容格式 / DeepSeek)的**落地实现**(仅预留接口,不实现)。
- 本地模型后端。
- Markdown 渲染、代码语法高亮、diff 预览、侧边状态栏等富界面元素。
- slash 命令、MCP、子 agent、工具并行执行。
- 配置文件体系(本轮配置仅来自环境变量与启动参数)。
